export const lit: ShaderGeneratorLit;
declare class ShaderGeneratorLit extends ShaderGenerator {
    generateKey(options: any): string;
    /**
     * @param {import('../../../platform/graphics/graphics-device.js').GraphicsDevice} device - The
     * graphics device.
     * @param {object} options - The options to be passed to the backend.
     * @returns {object} Returns the created shader definition.
     * @ignore
     */
    createShaderDefinition(device: import('../../../platform/graphics/graphics-device.js').GraphicsDevice, options: object): object;
}
import { ShaderGenerator } from './shader-generator.js';
export {};
